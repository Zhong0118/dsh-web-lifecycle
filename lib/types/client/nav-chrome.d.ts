/**
 * Official settings nav hard-codes icons by section id (gear for unknowns).
 * Paint this plugin's row as a red power switch after the shell mounts it.
 */
export declare function startNavChrome(): () => void;
